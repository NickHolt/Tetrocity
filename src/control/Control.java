package control;

/** An enum that lists all controls recognized by the Engine of a game of 
 * Tetrocity. 
 * 
 * @author Nick Holt
 *
 */
public enum Control {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    DROP,
    ROTATE_CLOCKWISE,
    ROTATE_COUNTERCLOCKWISE,
    STORE
}
